export function renderDraftReviewFooter(): string {
    return `
        <div class="draft-review-footer">
            Phiếu xem trước phục vụ rà soát trước khi gửi duyệt.
            Nội dung này chưa phải kết quả được phê duyệt.
        </div>
    `
}
